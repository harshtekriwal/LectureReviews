<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginactivity">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".OverallRatingActivity"></activity>
        <activity android:name=".CheckoutRatingActivity" />
        <activity android:name=".LectureFeedback" />
        <activity android:name=".WelcomeStudent" />
        <activity android:name=".StudentLoginActivity" />
        <activity android:name=".GenerateOTP" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WelcomeTeacher" />
        <activity android:name=".registeractivity" />
        <activity android:name=".MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>